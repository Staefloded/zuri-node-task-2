{
  "name": "file-system",
  "version": "1.0.0",
  "description": "Fetching data from an external API and storing it locally in a JSON file",
  "main": "file_system.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Eleyowo Michael",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1"
  }
}
